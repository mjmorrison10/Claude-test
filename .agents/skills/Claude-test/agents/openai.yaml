interface:
  display_name: "Claude Test"
  short_description: "Repo-specific patterns and workflows for Claude-test"
  default_prompt: "Use the Claude-test repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true